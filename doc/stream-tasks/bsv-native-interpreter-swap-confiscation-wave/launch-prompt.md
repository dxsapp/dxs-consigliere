Execute `SC1 -> SC2 -> A1` for the bounded native interpreter proof wave.

Rules:
- stay in `verification-and-conformance` and `repo-governance`
- prefer vendored deterministic fixtures over new harnesses
- update oracle hashes and evidence in the same commit wave
- rerun the focused native parity and lifecycle packs before closing audit
