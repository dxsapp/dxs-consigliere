# Audit Gate A4

## Trigger

- Triggered after: `S29`
- Focus:
  - business projection correctness
  - API compatibility drift
  - realtime contract quality
  - end-to-end performance envelope

## Inputs

- Plan: `/Users/imighty/Code/dxs-consigliere/doc/platform-api/vnext-implementation-slices.md`
- Ledger: `/Users/imighty/Code/dxs-consigliere/doc/stream-tasks/consigliere-vnext/master.md`
- Relevant commits:
  - `aab2109` vnext startup diagnostics and packaging controls
  - worktree changes in `tests/**` for full-system validation and benchmark harness tuning
- Evidence:
  - `/Users/imighty/Code/dxs-consigliere/doc/stream-tasks/consigliere-vnext/benchmarks/S29-full-system-benchmarks-evidence.md`

## Quality Review

- Status: `completed`
- Findings:
  - `good` Full-system validation now exercises replay, reorg, and deterministic repeated rebuild behavior over the vnext pipeline using direct projection document loads. This keeps the suite focused on vnext projection correctness instead of Raven auto-index side effects.
  - `good` The benchmark harness now emits replay/query/soak evidence successfully under the reduced `S29` scenario and confirms that the vnext path is runnable end to end on the current repository/runtime stack.
  - `watch` Reorg validation had to be expressed as a two-phase sequence: first persist confirmed tx applications, then append `block_disconnected`, then rebuild again. In the current address/token projection implementation, a disconnect fact processed in the same rebuild session as the original confirmations does not observe freshly stored `AppliedTransactionDocument` rows.
  - `watch` Current token reorg semantics remain conservative but imperfect: under the validated scenario the token balance projection for the issuer remains present while token state recomputes to `TotalKnownSupply = 0`. This is recorded as current behavior, not as the target business invariant.

## Reuse And Duplication Review

- Status: `completed`
- Findings:
  - `good` `S29` reuses production projection rebuilders and projection documents instead of inventing synthetic state calculators.
  - `accepted` The validation and benchmark projects each keep small scenario-local seed helpers. The duplication remains bounded and clearer than a shared cross-project fixture layer under the current runtime constraints.

## AI-First Review

- Status: `completed`
- Findings:
  - `good` Ownership boundaries held: `S29` stayed inside `tests/**` and did not mutate production zones to force the validation green.
  - `good` The full-system suite now has explicit seams that are cheap for future agents to reason about:
    - `VNextFullSystemValidationTests`
    - `VNextFullSystemBenchmarkHarness`
    - benchmark evidence file in the stream task package
  - `watch` Environment remediation was required to clear NuGet caches and recover disk headroom for embedded Raven. This was an operator/runtime condition, not a repository logic change, but it should remain visible in the ledger for future replay on constrained machines.

## Performance Evidence Review

- Status: `completed`
- Evidence:
  - `/Users/imighty/Code/dxs-consigliere/doc/stream-tasks/consigliere-vnext/benchmarks/S29-full-system-benchmarks-evidence.md`
- Measurements:
  - replay throughput: `10.31/s` for `4` logical replay operations
  - query throughput: `875.00/s` for `14` logical query operations
  - soak throughput: `500.00/s` for `6` logical soak operations
- Bottlenecks:
  - `Address/token projection replay was able to advance from the bounded emergency scenario back to `TransferCount = 4` after `R-A4-01`, confirming that page-level preloading and deferred token recompute materially reduced Raven session request pressure before `S30`.
  - token-state recomputation still depends on Raven query-heavy recompute paths rather than a more bounded materialized-state update model.

## Required Fix Plan

| remediation | issue | owner | status | validation | must_finish_before |
|---|---|---|---|---|---|
| `R-A4-01` | Reduce address/token projection rebuild request amplification so replay scenarios at least `TransferCount >= 4` pass without Raven session request-ceiling failures. | `operator/state` | `done` | `VNextFullSystemBenchmarkSmokeTests` with `TransferCount = 4` now passes and benchmark evidence was refreshed. | `S30` |

## Outcome

- Gate status: `passed`
- Continue blocked by: `none`
