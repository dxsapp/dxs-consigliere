# Audit Gate A5

## Trigger

- Triggered after: `S31`
- Focus:
  - cutover safety
  - rollback viability
  - legacy-path retirement risks
  - operator ergonomics for vnext startup and troubleshooting

## Inputs

- Plan: `/Users/imighty/Code/dxs-consigliere/doc/platform-api/vnext-implementation-slices.md`
- Ledger: `/Users/imighty/Code/dxs-consigliere/doc/stream-tasks/consigliere-vnext/master.md`
- Relevant commits:
  - `5ac45e4` batched legacy transaction persistence for `S30`
  - worktree changes for `S31` journal-first tx/block ingress and cutover-mode gating
- Validation:
  - `build:Dxs.Consigliere.Tests(useapphost=false)`
  - `vstest:TxObservationJournalMirrorBackgroundTaskTests|BlockObservationJournalMirrorBackgroundTaskTests|TransactionFilterJournalFirstTests`

## Quality Review

- Status: `completed`
- Findings:
  - `good` Tx observations now enter the journal inline from `TransactionFilter` before the legacy compatibility store is invoked. This gives `S31` a real journal-first edge instead of relying on the old mirror-only background flow.
  - `good` Block connected observations now append inline during block scheduling in journal-first modes, while block disconnect observations were already emitted directly by `BlockProcessExecutor`.
  - `watch` The inline block-connected path is currently covered by compile validation and mode-aware mirror tests, but there is no dedicated direct unit test for `BlockProcessBackgroundTask` journal-first scheduling order yet.

## Reuse And Duplication Review

- Status: `completed`
- Findings:
  - `good` Shared `TxObservationJournalWriter` and `BlockObservationJournalWriter` services now own observation serialization/fingerprint/payload behavior, so the inline path and mirror fallback do not duplicate protocol mapping logic.
  - `good` Mirror tasks were converted into mode-aware fallbacks rather than forked into separate codepaths, preserving rollback readability.

## AI-First Review

- Status: `completed`
- Findings:
  - `good` The cross-zone seam is explicit: `Dxs.Bsv` only gained the thin `ITxObservationSink` abstraction, while journal specifics stayed in `Dxs.Consigliere`.
  - `good` Ownership boundaries remain understandable for future agents because the new cutover logic is centralized in a small set of files instead of being smeared through controllers or data models.
  - `watch` `BlockProcessBackgroundTask` now carries one more orchestration responsibility. This is still coherent for the current slice, but future work should avoid letting it accumulate additional cutover policy.

## Cutover And Rollback Review

- Status: `completed`
- Findings:
  - `good` Explicit cutover modes now exist: `legacy`, `mirror_write`, `shadow_read`, `vnext_default`.
  - `good` Rollback remains straightforward: `legacy`/`mirror_write` keep the old async mirror tasks active and bypass inline journal-first behavior; `shadow_read`/`vnext_default` enable inline journal writes.
  - `good` The legacy compatibility store is still present after inline journal writes, so rollback does not require reconstructing compatibility state from scratch.

## Performance Review

- Status: `completed`
- Findings:
  - `good` Journal-first ingress does not add a second observation-mapping implementation; this avoids unnecessary CPU drift between modes.
  - `watch` Inline journal append now sits directly on the transaction-filter hot path. Current slice validation proves correctness and ordering, but a later perf wave should benchmark queue pressure under burst mode with journal-first enabled.

## Outcome

- Gate status: `passed`
- Continue blocked by: `none`

## Fix Plan

- `none_required` No blocking remediation is required before `S32`.
- `follow_up_watch_01` Add a direct unit/integration test for journal-first block-connected scheduling through `BlockProcessBackgroundTask` when the next runtime/perf wave touches that area.
- `follow_up_watch_02` Capture burst-mode perf evidence with journal-first ingest enabled before legacy retirement work starts.

## Required Remediation Slices

- `none`

## Validation Evidence

- `build:Dxs.Consigliere.Tests(useapphost=false)` passed against the `S31` worktree.
- `vstest:TxObservationJournalMirrorBackgroundTaskTests|BlockObservationJournalMirrorBackgroundTaskTests|TransactionFilterJournalFirstTests` passed with `6/6` tests green.
