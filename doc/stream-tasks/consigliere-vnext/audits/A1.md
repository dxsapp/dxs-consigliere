# Audit Gate A1

## Trigger

- Triggered after: `S08`
- Focus:
  - config and benchmark harness quality
  - journal abstraction cleanliness
  - payload-store abstraction reuse quality

## Inputs

- Plan: `/Users/imighty/Code/dxs-consigliere/doc/platform-api/vnext-implementation-slices.md`
- Ledger: `/Users/imighty/Code/dxs-consigliere/doc/stream-tasks/consigliere-vnext/master.md`
- Relevant commits:
  - `58008fe` benchmark and replay harness scaffold
  - `27f3872` source and storage config binding
  - `25e59da` observation journal primitives
  - `2285264` Raven raw transaction payload store
  - `da22f32` Raven observation journal

## Quality Review

- Status: `completed`
- Findings:
  - `resolved` Benchmark evidence originally emitted locale-dependent decimal formatting, which made throughput snapshots awkward to diff and parse across environments.
  - `accepted` `RavenObservationJournal<TObservation>` keeps sequence allocation logic close to append semantics instead of delegating to `RavenJournalSequenceAllocator`; this duplicates a small amount of code but preserves atomic dedupe-plus-sequence behavior in one cluster session.

## Reuse And Duplication Review

- Status: `completed`
- Findings:
  - `resolved` .NET runtime detection for Raven-backed suites was duplicated in multiple test classes; it is now centralized in `tests/Shared/DotNetRuntimeFacts.cs`.
  - `accepted` Raw payload and observation journal both keep deterministic id builders in their own model types; the duplication is shallow and domain-local, so no shared id utility is required yet.

## AI-First Review

- Status: `completed`
- Findings:
  - `good` Ownership boundaries held for Waves A-B: bootstrap/config, verification, state/storage, and platform-common were committed in separate slices.
  - `good` New seams are cheap-context:
    - `Consigliere:Sources` / `Consigliere:Storage` option models
    - `IRawTransactionPayloadStore`
    - `Dxs.Common.Journal` primitives
    - Raven-backed journal implementation isolated under `Data/Journal`
  - `watch` Benchmark validation currently relies on a dedicated local runtime home at `/Users/imighty/.dotnet-vnext`; this is acceptable for now but must remain explicit in evidence and docs.

## Performance Evidence Review

- Status: `completed`
- Evidence:
  - `/Users/imighty/Code/dxs-consigliere/doc/stream-tasks/consigliere-vnext/benchmarks/S08-journal-benchmarks-evidence.md`
- Bottlenecks:
  - append throughput baseline is currently the slowest path at `103.90/s` for the benchmark scenario
  - duplicate handling is cheap relative to append at `2285.71/s`
  - replay throughput is materially higher than append at `5333.33/s`

## Required Fix Plan

| remediation | issue | owner | status | validation | must_finish_before |
|---|---|---|---|---|---|
| R-A1-01 | centralize Raven-backed runtime detection for tests and benchmarks | operator/verification | done | benchmark + targeted integration test runs | Wave C |
| R-A1-02 | write benchmark evidence with invariant culture and document runtime path | operator/verification | done | evidence snapshot regenerated | Wave C |

## Outcome

- Gate status: `passed`
- Continue blocked by: `none`
