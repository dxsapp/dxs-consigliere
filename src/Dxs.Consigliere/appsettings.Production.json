{
  "_comment": "wave-A3 S5 — production overlay. Every credential is sourced from the Docker secrets mount (`/var/lib/consigliere/secrets/providers.json` for provider keys, env vars for everything else). Plaintext API keys NEVER live in this file; CI's secrets-lint step fails red if a future edit reintroduces them.",
  "BackgroundTasks": {
    "DisabledTasks": [
      "JungleBusSyncMissingDataBackgroundTask"
    ]
  },
  "ScanMempoolOnStart": true,
  "BlockCountToScanOnStart": 2,
  "RavenDb": {
    "Urls": ["${RAVENDB_URL}"],
    "DbName": "${RAVENDB_NAME}"
  },
  "Consigliere": {
    "AdminAuth": {
      "sessionTtlMinutes": 480,
      "cookieName": "consigliere_admin",
      "cookieSecure": "Always"
    },
    "Secrets": {
      "Dir": "/var/lib/consigliere/secrets"
    }
  }
}
