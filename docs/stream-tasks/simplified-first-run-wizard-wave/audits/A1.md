# simplified-first-run-wizard — A1 audit

First audit cycle. Branch `codex/consigliere-vnext`. Range `49e885a..6d03bf0`.

## What landed
- **S1 (`1843bf4`)** — `SetupWizardService.CompleteAsync`: Providers + BlockSync
  optional. Admin-only request completes on seeded p2p-primary defaults, skips
  provider-config apply, requires NO JungleBus subscription, still enables P2P.
  Legacy full request still validates+applies; partial block sync still
  rejected. No DTO/contract change.
- **S2 (`6d03bf0`)** — wizard UI collapsed to a single admin-account step;
  Providers/Block-sync/Review steps removed from the flow (3 step components
  deleted, no external importer); store sends admin + empty providers/blockSync;
  copy reframed to the no-subscriptions story.

## Validated
- `dotnet build -c Release` clean; SetupWizardServiceTests 5/5 + SetupControllerTests.
- `pnpm verify` green (199 unit, build, budget under cap, contracts:check OK);
  `test:contract` 24/24; `secrets-lint` 0.

## Residuals
- **Live first-run unverified end-to-end** (fresh stack → one-step wizard →
  app, no subscription) — operator-run, evidence-pending. Logic + units cover it.
- Providers + block subscription remain configurable via the existing Settings
  screens (unchanged) — not re-verified here beyond "still builds".
- The empty-objects contract between S2 (`buildRequest`) and S1
  (`hasProviderSelection`) is the integration seam — covered by S1's unit but
  worth an eyeball in audit.

## Audit prompts
`audits/S1-slice-audit-prompt.md`, `audits/S2-slice-audit-prompt.md`. No
MAJOR/HIGH findings folded as of closeout.
